version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist   # Vite outputs production files here
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*